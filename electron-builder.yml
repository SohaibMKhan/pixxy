Exit code: 0
Wall time: 0.6 seconds
Output:
appId: com.sohaibmkhan.pixxy
productName: Pixxy
artifactName: Pixxy-${version}-${arch}.${ext}

directories:
  output: release
  buildResources: assets

files:
  - out/**
  - package.json

win:
  target:
    - nsis
  artifactName: Pixxy-${version}-Windows-${arch}.${ext}

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Pixxy

publish: null

